<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foragentss">

    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.hardware.camera" />
    <uses-permission android:name="android.hardware.camera.autofocus" />

    <application
        android:name=".auth.helpers.SuperApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".auth.retailers.activities.PrinterActivity"></activity>
        <activity android:name=".auth.retailers.activities.MyProfileActivity" />
        <activity android:name=".auth.retailers.activities.QRCodeShowerActivity" />
        <activity android:name=".auth.retailers.activities.DownloadActivity" />
        <activity android:name=".auth.agents.activities.SendCardActivity" />
        <activity android:name=".auth.agents.activities.RetailersActivity" />
        <activity android:name=".auth.agents.activities.PartnersShowerActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".auth.retailers.activities.RetailerActivity" />
        <activity android:name=".auth.retailers.RetailersDashboard" />
        <activity android:name=".auth.agents.activities.CardRequestActivity" />
        <activity
            android:name=".auth.agents.AgentsDashboard"
            android:label="@string/title_activity_agents_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".home.LoginActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".home.RegistrationActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>